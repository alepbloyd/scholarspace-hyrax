pipeline {
    agent any
    stages {
        stage('switch scholarspace folder') {
            steps {
                sh 'ls -l'
                sh 'pwd'
                sh 'whoami'
                sh 'ls -l /'
            }
        }
    }
}

// clobber assets
// precompile assets
// passenger-config restart-app /